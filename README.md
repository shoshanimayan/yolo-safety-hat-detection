# yolo-safety-hat-detection
python yolo program for detecting if a person has a safety helmet on or not, one version of the program is for for static images, the other is realtime detection using a webcam
